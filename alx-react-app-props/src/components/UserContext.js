import { createContext } from "react";

 HEAD
const UserContext = createContext(null); // Create the context
export default UserContext;              // Export it for other components

const UserContext = createContext(null);  // Create a context object

export default UserContext;               // Export it for use in other components
 d4c483d (Move UserContext.js into components folder)
